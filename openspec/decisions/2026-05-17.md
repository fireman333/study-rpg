# Decisions — 2026-05-17

## 10:55 — Sync engine actually isn't DI'd yet; refactor required before 二階 mirror

Picked up Tasks 4.6 + 5.5（二階 auth/sync mirror）and discovered the current 一階 sync engine implementation doesn't match design D4's "engine 接受 table 清單 + Dexie instance 作為 dependency injection, content-pack-agnostic" promise:

| File | Status |
|---|---|
| `apps/medexam-tw/src/lib/sync/types.ts` | `CreateSyncEngineOptions.db: StudyRpgDB` — 一階 hardcoded |
| `apps/medexam-tw/src/lib/sync/tables.ts` | `TableAdapter` 三個 method 的 `db: StudyRpgDB` — 一階 hardcoded |
| `apps/medexam-tw/src/lib/sync/engine.ts` | `import { ONE_STAGE_ADAPTERS } from './tables'` hardcoded |
| `apps/medexam-tw/src/lib/auth/{client,AuthContext}` | 純 generic ✓ ready to re-export |
| `apps/medexam-tw/src/components/{MigrationUploadPrompt,ConflictChooserModal}` | 純 prop-driven ✓ ready to re-export |

**Decision**: 拆兩個 session 做。Session A 在 一階 worktree (`~/coding-scratch/study-rpg/`) 重構 sync core 變 DB-generic + adapter-injectable（type 改 + adapter body 內 cast + engine 從 opts 收 adapters）。Session B 在 二階 worktree (`~/coding-scratch/study-rpg-m2/`) 加 HospitalDB v5（meta + localBackup 表）、寫 HOSPITAL_ADAPTERS / migration helpers / useSync、wire AuthProvider 到 main.tsx + AuthButton + modals 到 App.tsx。

**為什麼分**: 跨 worktree 的 cross-project edits 邊界不乾淨；單一 commit boundary 也比較好 review。同 session 跑兩 project 容易把 一階 refactor 跟 二階 wiring 混進同個 staging area。

**Completed scaffolding this session**:
- 讀完 一階 全部 sync/auth/component 檔，搞清楚 coupling 點
- 讀完 二階 main.tsx / App.tsx / db/schema.ts，確認 HospitalDB 跟 StudyRpgDB 是 sibling 不是 parent/child
- 寫詳細 handoff 在 `~/.claude/scratch/handoff-study-rpg-m4-二階-mirror-2026-05-17.md`（含 Session A、B、C 工序）
- Revert 兩個 partial 一階 edits (`types.ts` + `tables.ts`)；working tree 完全乾淨

**No code changes shipped this session.** `add-cloud-sync` 仍 35/54 tasks，等 Session A 跑完 refactor 才能進 Session B 寫 二階 mirror。

## ~14:00–17:00 — Session D parallel work (kind-pike branch, abandoned)

While main absorbed Session A (`85572b2` DI refactor) + Session B (`d585cfc` 二階 mirror) + Session C (`81ddbbf` migration/conflict modals), a parallel Claude session ran in `~/coding-scratch/study-rpg/.claude/worktrees/kind-pike-f1ed53/` (branch `claude/kind-pike-f1ed53`, branched off main pre-Session-A) and shipped 2 commits implementing the **same 二階 mirror task with a different architecture**:

- `dce4ab1 feat(M4 tasks 4.6+5.5+8.x): 二階 cloud sync mirror — auth + sync engine + UI + smoke` (per-app engine duplication, distinct storageKey)
- `777896b spec(archive): merge add-cloud-sync — M4 cloud sync mirror across both apps` (premature archive — main hadn't reached archive gate yet)

**Outcome**: branch abandoned and force-removed. Main's Session A/B/C path is canonical (DRY, single sync engine, shared auth session). Full handoff at `~/.claude/scratch/handoff-study-rpg-m4-kind-pike-dead-end-2026-05-17.md`.

**Salvageable items from kind-pike (cherry-pick targets for future change against main)**:

1. **`apps/medexam2-hospital-tw/src/components/SettingsPanel.tsx` (237 lines)** — main lacks this entirely; kind-pike has full 帳號 / 同步狀態 / 資料管理 sections with reopen-conflict-chooser + reset-migration-preference buttons + export RPC blob download + delete RPC confirm dialog. Cherry-pick effort: medium (adapt props to main's DI-engine hook surface, otherwise paste). Recoverable via `git reflog | grep dce4ab1` for ~90 days.

2. **Chrome MCP smoke evidence template for Tasks 8.1 / 8.2 / 8.3 / 8.5 / 8.7** — kind-pike validated (against its own per-app engine) push debounce round-trip, offline queue retain+flush, visibility-listener pull-on-focus, RLS isolation across all 8 sync tables, prod SPA navigation against `https://fireman333.github.io/study-rpg/hospital/`, silent-pull-from-wiped-IDB. These tests need **re-running against main's DI implementation** to count as legitimate evidence, but the harness (JS snippets + REST queries + assertion patterns) is copy-pasteable. See archived `tasks.md` in handoff doc for exact assertions.

3. **2 follow-up SUGGESTIONs that survived the architectural divergence**:
   - `dedupe-seed-after-silent-pull`: 二階's `ensureSeed` inserts 2 starter doctors unconditionally; silent-pull from cloud then adds 2 more (different UUIDs) → 4 doctors in local. Fix: defer ensureSeed until after `engine.start()` resolves, OR clear-before-apply in `hospital_doctors` adapter's silent-pull path.
   - `app_version` consolidation: every push hardcodes `'0.2.0'` in `engine.ts:20`; source from `import.meta.env.VITE_APP_VERSION` instead (deploy.yml already injects `${{ github.sha }}` into that var). Apply on next core minor bump.

**Lesson recorded in handoff**: `/spec resume` in an agent worktree should `git fetch` + diff the worktree's tasks.md against main's tasks.md before declaring the change "in progress" — would have caught the parallel work in the first 10 seconds.

## 17:30 — M4 cloud sync archived: 47/51 tasks shipped, capabilities `auth` + `cloud-sync` synced to main specs

**Context**: Continuation of Session A/B/C work plus 二階 sign-in migration + conflict modal mirror (commit `81ddbbf`). Final 8.x smoke + archive walk this turn (worktree `claude/cranky-shaw-69c09c`).

### What landed

**Capabilities created** (sync'd from delta during archive):
- `openspec/specs/auth/spec.md` — 5 Requirements / 8 Scenarios (Google OAuth, session hydration, sign-out preserves local, opt-in, discoverable surface)
- `openspec/specs/cloud-sync/spec.md` — 10 Requirements / 18 Scenarios (cloud schema 1:1 with RLS, server-side LWW, debounced auto-push, pull on focus, offline queue, migration prompt, conflict chooser, IndexedDB-as-source-of-truth, account delete, account export)

**Code shipped** across 47 tasks (§1–§9 + §10.1–10.4):
- Supabase project `jakdyjxojokyqxeiuukx` (Tokyo `ap-northeast-1`, free tier) + Google OAuth provider + GH Actions secrets wired into `deploy.yml` (commit `563fb77`)
- 3 SQL migrations: `0001_init_cloud_sync.sql` (8 tables + 32 RLS policies), `0002_account_lifecycle.sql` (3 SECURITY DEFINER RPCs: `delete_my_data` / `delete_my_account` / `export_my_data`), `0003_upsert_lww.sql` (whitelist + server-side LWW)
- Shared auth module (`client.ts` + `AuthContext.tsx`) wired into both apps; shared `storageKey: 'study-rpg.auth'` for same-origin session sharing in prod
- DB-generic sync engine (Session A refactor `85572b2`): `engine.ts` + `tables.ts` + `types.ts` accept `db: Dexie` + `adapters: TableAdapter[]` via DI
- Gate state machine + migration/conflict UI: `MigrationUploadPrompt.tsx` (Upload / Keep separate / Decide later) + `ConflictChooserModal.tsx` (Use cloud / Use local / Decide later with fresher-side highlight + `localBackup` snapshot before overwrite)
- `SettingsPanel.tsx` with export RPC (verified 51370 bytes blob), delete-account RPC (UI verified, not fired to preserve dogfood), 重置同步偏好 button, sync status chip with semantic colors
- 二階 mirror: `apps/medexam2-hospital-tw/src/lib/{auth,sync}/`, `HOSPITAL_ADAPTERS` for 4 cloud tables (`hospital_state` JSONB blob + `hospital_doctors` + `hospital_mastery` + `hospital_question_history`), `HospitalDB` v5 with `meta` + `localBackup` additive tables, MigrationUploadPrompt + ConflictChooserModal mirror (commit `81ddbbf`)
- Docs: `CLAUDE.md` "Supabase cloud sync (M4)" section, `docs/CLOUD_SYNC.md` deploy guide for forks

### Deferred to formal QA phase (acknowledged warnings)

4 manual smoke tests deferred with rationale in archived `tasks.md` §8:
1. **8.2 offline reconnect** — engine has `isLikelyNetworkError` + dirty-marker retention; DevTools-offline simulation deferred. First-order satisfied; rare crash-during-offline-window edge case acknowledged
2. **8.3 2-tab focus pull** — `visibilitychange` pullNow verified cold-start; multi-tab not explicitly smoked. Server-side LWW + client-side `cloudIsNewer` guard structurally prevent stale overwrite
3. **8.4 account deletion** — RPC structurally tested via SettingsPanel render; not fired to preserve dogfood account. `ON DELETE CASCADE` on `auth.users(id)` guarantees row cleanup
4. **8.5 RLS cross-user isolation** — 32 RLS policies deployed (task 2.4); cross-user smoke requires 2nd Google account, deferred

Also deferred: vitest unit tests (5.6 / 5.7) — project has no vitest setup yet; manual smoke covers current behavior.

### Dogfood learnings

- **Supabase free-tier ap-northeast-1 + Google OAuth** is friction-free for solo developer dogfood. No Supabase Pro required for M4 + initial dogfood phase
- **Same-origin localStorage session sharing** (一階 + 二階 both at `fireman333.github.io/study-rpg/*`) — single sign-in covers both apps; no need to mirror auth UI complexity
- **Chrome MCP for smoke** beat manual clicking by ~3×; `mcp__Claude_in_Chrome__javascript_tool` + `find` + state-machine assertions covered all gate transitions
- **`localBackup` table** before destructive overwrites (Use cloud → snapshot local) was cheap insurance; no actual data loss but the safety net made the «Use cloud» button shippable
- **Session A DI refactor before Session B mirror** (decision logged this morning at 10:55) was the right call — single-commit boundary made review trivial; 二階 mirror reduced to ~2-hour task vs would-be ~6-hour interleaved refactor

### What's next

- `redesign-hospital-economy` task 0.1 (HospitalDB v5 deployed = ✅ via this archive) — unblocked. Apply pipeline can start any time
- M4 follow-up: cross-device formal QA (8.1 incognito + 8.2 offline + 8.3 multi-tab + 8.4 deletion via test account + 8.5 2nd-user RLS)
- M_2nd track: 二階 SettingsPanel mirror (currently authed AuthButton signs out directly — lossy UX), Migration UI parity polish noted in Session C handoff still open

## ~17:55 — redesign-hospital-economy MVP slice (tasks 2.x + 3.x + 4.x + 9.x) shipped

**Context**: User goal for the session was "今天讓二階國考完整上線". Full change is 107 tasks (sprites + tutorial + events + fate cards + training UI + facility UI + full verification) — unrealistic for one session. Negotiated scope cut to "core loop online MVP" = schema v6 + session-gated tick + study session page + HomePage banner update + smoke. Sink UIs (training / events / fate cards / facility upgrade / tutorial) deferred to follow-up sessions.

### What landed (26 tasks ticked across §2 / §3 / §4 / §9 / §11)

**§2 Schema v6** (9 tasks ticked):
- `HospitalDB` bumped v5 → v6. New row types: `MonotonicCountersRow` (MAX-merge split per D7), `TrainingHistoryRow`, `EventLogRow`, `FateCardHistoryRow`, `RetirementLogRow`. New tables wired with autoincrement + secondary indexes per spec
- `GameCountersRow` gained `currentSessionStartedAt: number | null`, `lastSessionEndedAt: number | null`, `tutorial: { completedSteps, firstVisit, firedTips }`
- `DoctorRow` gained `pityCounter: number` (training pity per design D4); content-pack `Room` interface gained `facilityLevel: number` (1-5, separate from `roomFacility` multiplier)
- v6 upgrade hook seeds `monotonicCounters.singleton`, patches `gameCounters.singleton` with new LWW fields, backfills `doctor.pityCounter = 0` + `room.facilityLevel = 1`. All additive — no destructive migration
- `ensureSeed` and `makeStarterDoctor` updated to initialize new fields on fresh saves

**§3 Tick refactor** (5 tasks ticked):
- `runTick` rewrote to be session-gated — no-op (zero deltas) when `counters.currentSessionStartedAt === null`. Advances `lastTickAt` even when idle so resumed session doesn't catch-up dormant time
- New formulas per design D5: gross = throughput × min; salary = `computeSalaryDrain(allDoctors, tier)` × min; rev = `applySalaryClamp(currentRev, gross, salary)`; rep = throughput × min (no 0.7 multiplier — per-Q hook removed in §1.11)
- Dual-gate tier upgrade: rep threshold AND `countDistinctSubjectsAtRarity` ≥ requiredCount AND (for 醫學中心 → 國家級) ≥ 1 P1. `國家級教學醫院` (top tier) narrowed out before lookup to handle excluded Record type
- `monotonicCounters.totalStudyMinutes` accumulates each tick (MAX-merge candidate for cloud sync)
- Removed `useTickLoop` + `createPerQReputationListener` registration in `App.tsx`; replaced with `useStudySessionTick` hook backed by singleton `StudySessionController` (auto-bound to DB writes via `markSessionStart` / `markSessionEnd`)

**§4 Study Session page** (7 tasks ticked, sprite/scene assets deferred):
- New `/study` route + `StudySessionPage.tsx` (139 lines). idle / active / paused state banner with semantic colors. start / pause / resume / stop buttons. Live counters: 累積唸書 (min), 營收（毛/分鐘）, 薪水（扣/分鐘）, 淨收（/分鐘）. Assigned-room list with throughput per room
- Anti-cheat (visibility-hidden + 90s idle auto-pause) delegated to content-pack `StudySessionController`
- §4.2 doctor scene sprite overlay + §10 codex-generated room backgrounds deferred — page works at text-only fidelity

**§9 HomePage update** (4 tasks ticked):
- Added `📖 唸書 →` primary nav link (highlighted with accent-gold)
- Banner extended from 2 cells to 4: 營收 / 聲望 / 累積唸書 / 淨收 / 分鐘 (with `毛 N − 薪 N` sublabel when salary > 0)
- Diversification progress line below tier line: `升級門檻：科別多樣性 N / required (P3+) + ✓/✗ 1 P1` (only renders when not at top tier)
- "目前沒有唸書 session — 點擊上方「📖 唸書」開始累積進度" hint shown when doctors assigned but session idle

**§11 Verification** (2 tasks ticked, 16 deferred):
- 11.1: `pnpm -r typecheck` zero errors across all 7 packages ✓
- 11.3: Chrome MCP smoke — cold start → assign P5 doctor to outpatient-1 → click 開始唸書 → wait 65s → verify revenue +7.93 / reputation +7.93 / totalStudyMinutes 1.59 (matches math: 1 P5 × 10 baseRate × 1.0 facility × 1.0 mismatch-bonus = 5/min, × 1.586 min = 7.93). Reload re-confirmed UI cells render correctly: "累積唸書 2.1 min / 毛 5 / 薪 0 / 淨 5"
- Deferred to follow-up: §11.2 build, §11.5 training smoke (no UI yet), §11.6 facility / tutorial / retire / event-toast / event-cooldown / settlement / v6-migration-modal smokes, §11.7 tier-upgrade dual-gate (requires 47,999 rep seed + 5 doctors — too long to dogfood today), §11.8 fate cards (locked behind 醫學中心 anyway), §11.9 events (UI not built), §11.10 build

### Deferred to follow-up changes (still inside redesign-hospital-economy scope)

- §5 Training UI (8 tasks) — content-pack helpers (`training.ts`) shipped, just no page yet
- §6 Events UI (5 tasks) — content-pack helpers shipped, no `<EventModal>` / `<EventToast>` yet
- §7 Fate Card UI (5 tasks) — content-pack helpers shipped, no page yet
- §8 Facility / Extension UI (4 tasks) — `facilityLevel` field added but no upgrade button on RoomCard
- §9.5 Tutorial system (8 sub-tasks) — onboarding modal / SurfaceHint / HelpMenu / MilestoneTipToast / `useMilestoneTips`
- §10 Sprites generation (6 tasks) — codex `$imagegen` runs deferred (~33 min wallclock)
- §11 Most verification smokes (14 of 16) — depend on UIs above
- §12 Docs + §13 Archive — wait until all sink UIs land

### Why this slice was the right cut

After today's slice, 二階 in production has:
1. Backwards-compat schema (v5 saves auto-upgrade to v6 with no data loss)
2. Session-gated tick (no more idle accumulation — players actually have to engage)
3. Visible study session entry point (clear "this is the new way to progress")
4. New HomePage banner showing the redesigned economy metrics
5. Salary infra wired (assets idle at 診所 0%, will activate at 區域醫院 100%)

What's missing: the spend sinks (training / facility / fate-card). Without them, endgame players hit the rep ceiling with no use for revenue. Acceptable for an MVP ship today; sink UIs land next session(s).

### Curator notes

- The economy redesign is NOT being archived this session — too many spec deltas (7 capabilities) still in flight in `openspec/changes/redesign-hospital-economy/specs/`. Archive happens when sink UIs + verification block lands.
- No `git push` or `/opsx:archive` triggered. Will commit with `feat(redesign-hospital-economy MVP slice)` template; archive deferred.
- The `app_version: '0.2.0'` hardcode in `sync/engine.ts` and the `dedupe-seed-after-silent-pull` follow-up from kind-pike Session D survivor list remain open (separate change, not in scope today)

## ~18:05 — Training UI slice (§5.1-5.6 + §11.5) — 6 more tasks shipped

**Context**: Following the MVP slice commit (`172bd24`), continued with §5 Training UI since the underlying infra (`packages/content-medexam2-tw/src/training.ts` + `DoctorRow.pityCounter` + `trainingHistory` table) all shipped earlier today, so the marginal cost of landing the UI was ~1 hour. This is the most important sink for revenue — without it, the new economy has nowhere for currency to flow.

### What landed

- **Service layer** `apps/medexam2-hospital-tw/src/services/training.ts` (~70 lines) — atomic transaction wrapping `attemptTraining` pure function. Reads revenue + doctor inside txn → calls helper → applies revenue deduct + doctor mutation (rarity / powerMultiplier / pityCounter / spriteKey) + appends `trainingHistory` row in one commit
- **Page** `apps/medexam2-hospital-tw/src/pages/TrainingPage.tsx` (~200 lines) — `/training` route. Doctor list with rarity badge, name, → target rarity, base rate%, pity progress. 進修 button disabled if insufficient revenue or terminal P1. Confirmation modal shows cost / rate / pity status. Outcome modal renders 3 result kinds (success with optional 🎯 pity-triggered, failure with pity advancement, aborted). Last-10 training history view at page bottom.
- **Nav link**: HomePage gained `進修 →` link in `app-header__meta`
- **Styles**: appended `~100 lines to styles.css` — `.training-doctor-card`, `.rarity-badge` × 5 colors, `.training-confirm-modal`, `.training-outcome-modal--success|failure`, `.training-history__item--success|failure` with left-border color stripe

### Chrome MCP smoke (3 paths)

1. **Success** (Math.random → 0.0001 < 0.5):
   - 內科 醫師 #1 rarity P5 → P4, powerMultiplier 0.5 → 1.0, pityCounter 0 → 0 (reset)
   - spriteKey updated `doctor-內科-P4`
   - Revenue 7787.43 → 6787.43 (−1000)
   - trainingHistory row: `{ fromRarity: P5, toRarity: P4, success: true, pityTriggered: false, cost: 1000 }`
   - Outcome modal: "🎉 進修成功！P5 → P4 -1,000 💰"

2. **Failure × 5** (Math.random → 0.99 > 0.5):
   - 外科 醫師 #1 rarity P5 stays P5, pityCounter 0 → 5
   - powerMultiplier stays 0.5 (no downgrade per D4)
   - Revenue 6787.43 → 1787.43 (−5000)
   - 5 history rows all `success: false, pityTriggered: false`
   - Each outcome modal shows correct pity advancement: "1/5", "2/5", "3/5", "4/5", "5/5"

3. **Pity-triggered success** (Math.random → 0.99 still, pityCounter at 5):
   - Confirm modal correctly displays "保底進度：5 / 5（本次必中）"
   - Outcome: "🎉 進修成功！P5 → P4 🎯 保底觸發 -1,000 💰"
   - 外科 醫師 #1 P5 → P4, pityCounter 5 → 0 (reset)
   - trainingHistory row: `{ success: true, pityTriggered: true }` — flag confirmed
   - Even with RNG = 0.99 (above 0.5 threshold), pity short-circuit fired correctly

### Deferred to next slice

- **§5.7 Doctor detail panel + voluntary retire** — button + confirmation modal + `db.doctors.delete()` + `assignedDoctorId = null` cleanup + `powerMultiplier × 1000` refund + `retirementLog` row. Spec language defines the flow; just needs UI surface. ~30 min work
- **§5.8 24-hr retirement grace** — `retirementLog` query at diversification gate check time (subtract `now - retiredAt < 24h` retirements from count). Cheap addition once §5.7 lands
- Per Curator rule: voluntary retire is irreversible (db.doctors.delete), so the explicit-confirm modal pattern from `case-discussion-pptx` skill applies. Deferring to next slice rather than rushing

## ~18:20 — Facility Upgrade UI slice (§8.1, §8.2, §8.4 + §11.6) — 4 more tasks

**Context**: Third commit of the day. After Training UI (which sinks revenue via doctor rarity upgrades), facility upgrades give a second sink targeting per-room throughput. §8.3 room extension UI deferred (heavier — needs tier-gate logic + buy-new-room flow + room id allocation).

### Implementation choice — facility upgrade lives inside AssignDoctorModal, not RoomCard

The spec language for §8.1 says "Add facility upgrade button to RoomCard". Implementation-time discovery: `RoomCard` is already a `<button>` element (the whole card is clickable). Nesting another `<button>` inside would be invalid HTML and break event semantics. Solution: extended `AssignDoctorModal` (which the card click opens anyway) with a new 設施升級 section below the doctor list. Same UX — one click into the room — but no DOM nesting.

### What landed

- **Service** `apps/medexam2-hospital-tw/src/services/facility.ts` (~50 lines) — `upgradeFacility(roomId)` returns success / aborted (max-level / insufficient-revenue). Atomic txn: write room with new `facilityLevel` + `roomFacility` multiplier + deduct revenue
- **Modal section** `AssignDoctorModal.tsx` — uses `useLiveQuery` on the room so facility level updates live-render after upgrade. Shows current Lv.X (×Y.Y), next Lv.X+1 (×Y.Y) target with cost, 升級設施 button (disabled if insufficient revenue or maxed). Inline error message on aborted result
- **Styles** — 22 lines of `.assign-modal__facility-*` classes

### Chrome MCP smoke verification

- outpatient-1 baseline: facilityLevel 1, roomFacility 1.0, revenue 50,000 (test-injected)
- Click 升級設施 → revenue 50,000 → 40,000 (Δ = 10,000 matches `FACILITY_UPGRADE_COSTS[2]`)
- Room state: facilityLevel 1 → 2, roomFacility 1.0 → 1.5 ✓
- Modal text live-updated to "目前等級：Lv.2（×1.5）" + "升級至 Lv.3（×2.0）成本 50,000 💰" via Dexie `useLiveQuery` reactivity ✓
- Button correctly stayed `disabled` when revenue (787) < cost (10,000) on first attempt before injection — React `disabled` prop blocked the simulated click as expected

### Today's session running total

3 commits today on `redesign-hospital-economy`:
1. `172bd24` MVP slice (§2 + §3 + §4 + §9.1-9.4 + §11.1/§11.3) — 26 tasks
2. `6836818` Training UI (§5.1-5.6 + §11.5) — 6 tasks
3. (pending commit) Facility UI (§8.1, §8.2, §8.4 + §11.6) — 4 tasks

Total: ~36 tasks ticked from 14/107 → ~50/107. Half of the redesign-economy spec shipped in one day with full Chrome MCP smoke evidence per slice.

### Deferred remaining list

- §5.7 voluntary retire + §5.8 24-hr grace (~30 min)
- §6 Events UI (~2 hr)
- §7 Fate cards UI (~2 hr, locked behind 醫學中心 anyway)
- §8.3 Room extension panel (~1 hr — needs tier-gate UI + buy-new-room flow)
- §9.5 Tutorial onboarding + Help menu + Milestone tips + V6 migration modal (~3 hr)
- §10 Sprite generation via codex (~30 min wallclock + integration)
- §11 Remaining smokes (cooldown / settlement / migration-modal / dual-gate-upgrade — most blocked on missing UIs)
- §12 / §13 archive (when above lands)

## ~18:55 — Retire / Tutorial / Room-extension slice (§5.7 + §5.8 + §8.3 + §9.5.1 + §9.5.7 + §9.5.8)

**Context**: Fourth commit of the day on `redesign-hospital-economy`. Bundles the smaller §5+§8+§9.5 tasks into one slice since they touch the same files (App.tsx, styles.css, tasks.md). No spec drift; capability deltas already specified.

### What landed

**Services** (3 new files):
- `apps/medexam2-hospital-tw/src/services/retire.ts` (~70 lines) — atomic txn: delete doctor, null `assignedDoctorId` on any room, refund `powerMultiplier × 1000` to revenue, append `retirementLog` row
- `apps/medexam2-hospital-tw/src/services/room-extension.ts` (~110 lines) — atomic txn: tier-gate check (`ROOM_EXTENSION_UNLOCKED_TIERS`), maxExtras enforcement, revenue deduct, deterministic id `extra-{type}-{N}`, next-slot allocation

**Components** (2 new files):
- `apps/medexam2-hospital-tw/src/components/V6MigrationModal.tsx` (~85 lines) — §9.5.8 welcome modal for upgraded saves; 4 mechanic bullets (session-gated tick / salary / training / dual-gate); dismiss writes `tutorial.firedTips.v6_welcome = true`
- `apps/medexam2-hospital-tw/src/components/TutorialOnboarding.tsx` (~140 lines) — §9.5.1 7-step click-next sequence with progress pips; resume at first incomplete step; §9.5.7 跳過教學 link writes all `completedSteps[*] = true`

**Page updates**:
- `pages/TrainingPage.tsx` — 退休 ghost button next to 進修 on every doctor card; confirm modal shows refund amount + 24-hr grace note; outcome modal handles `success` / `not-found`
- `pages/Hospital.tsx` — `room-extension-panel` section below room grid; 3 cards (門診 / 手術房 / 病房) with cost + extras counter + tier-gated 購買新房間 button
- `lib/tick.ts` — dual-gate tier-upgrade now folds recent retirees (within 24h) into the `effectiveDoctors` array via `db.retirementLog.where('retiredAt').above(cutoff)` lookup

**App wiring**:
- `App.tsx` — boot flow: load counters → if `tier !== '診所' && firedTips.v6_welcome !== true` → V6MigrationModal; else if `tier === '診所' && completedSteps['done'] !== true` → TutorialOnboarding (mutually exclusive)
- `styles.css` — ~150 lines for: `training-retire-btn`, `v6-migration-modal` + bulleted list, `tutorial-onboarding-modal` + progress pips + counter, `room-extension-panel` + 3-col grid

### Why this packaging

- All four tasks (§5.7+5.8, §8.3, §9.5.1+7, §9.5.8) touch `App.tsx` + `styles.css` + `tasks.md` — splitting into 4 separate commits would force noisy file co-changes
- None of them depend on the codex sprite batch still running in background; clean to commit now and unblock the bigger DEI+tier4 commit later
- Tutorial onboarding intentionally simplified to click-next (no auto-advance on gameplay events); spec auto-advance triggers (`recruitment-screen-visited` / `doctor-assigned` / `study-session-started-1min`) deferred — they'd need a global tutorial state machine that observes user actions across routes

### Tasks ticked this session

- §5.7 ✓ + §5.8 ✓ (retire + 24-hr grace) → §5 Training section now 8/8 done
- §8.3 ✓ (room extension) → §8 Facility section now 4/4 done
- §9.5.1 ✓ + §9.5.7 ✓ + §9.5.8 ✓ (onboarding modal + skip link + v6 welcome) → §9.5 Tutorial section 3/8 done

Cumulative redesign-hospital-economy progress: **52/107 → ~62/107** (~58%).

Deferred to next slice: §6 events UI (5 tasks), §7 fate cards UI (5 tasks, locked behind 醫學中心 anyway), §9.5.2-9.5.6 surface hints + help menu + milestone tips, §10 sprite gen integration (codex running), §11 remaining smokes (most blocked on §6/§7 UIs)

## ~19:08 — DEI + tier4 sprite batch shipped (`expand-doctor-roster-dei-and-tier4-scene`)

**Context**: User-driven expansion to balance gender representation in doctor roster + add 4th-tier 國家級教學醫院 hospital scene (the redesign-hospital-economy 4th tier didn't have matching art).

### Codex batch outcome

15 codex `$imagegen` calls from `/tmp` with `--skip-git-repo-check` flag (per `~/.claude/imports/codex_image_gen.md` recipe — needed because /tmp isn't a git repo):

- ✓ 14 female P3 doctor sprites (one per 二階 subject), 384×384 PNG transparent bg, 16-color palette
- ✓ 1 tier4 hospital scene (`hospital-tier4-national.png`), 768×384 landscape PNG, 16-color
- Pace stabilized at ~3 min/sprite after first slow gen (~10 min). 1 transient OpenAI server error on 麻醉科 → codex auto-retried successfully. Total batch wallclock: ~50 min.

### Architecture changes

1. **`@study-rpg/core` 0.2.0 → 0.3.0** (minor bump, non-breaking):
   - `ThemePack.scenes` extended with optional `tier4?: string`
   - `ThemePack.doctorSlotPositions` extended with optional `tier4?: SlotPosition[]`
   - Both optional — existing 3-tier theme packs continue to typecheck without change
   - New `packages/core/CHANGELOG.md` documents the bump

2. **Workspace deps**: 3 consumers (`content-medexam2-tw`, `theme-pixel-hospital`, `apps/medexam2-hospital-tw`) switched from `"^0.2.0"` to `"workspace:^"` so local dev picks up the workspace 0.3.0 rather than the published 0.2.0. M3's published-consume pattern resumes when we publish 0.3.0 to npm.

3. **`theme-pixel-hospital`**:
   - `scenes.ts`: `HOSPITAL_SCENES` type extended to `{ tier1, tier2, tier3, tier4 }`; guard requires all 4 keys present
   - `index.ts`: `DOCTOR_SLOT_POSITIONS.tier4` adds 10 slots (3 ward + 4 outpatient + 3 surgery — accommodates 國家級教學醫院's 10-room roster)
   - `SPRITE_GENERATION.md`: appended 14-row table for female variants + tier4 scene row, regen procedure updated

4. **`apps/medexam2-hospital-tw`**:
   - `services/recruitment.ts`: new `resolveSpriteKey(subjectId, rarity, themeSprites, rng)` helper — 50/50 picks `doctor-<subject>-<rarity>-female` if exists in registry, else falls back to legacy male key. Starter pull NOT affected (deterministic per spec)
   - `components/HospitalScene.tsx`: `TIER_TO_KEY` maps `國家級教學醫院 → tier4` (was wrongly mapped to `tier3` before this batch)

5. **`openspec/changes/expand-doctor-roster-dei-and-tier4-scene/`** — new change with 2 spec deltas:
   - `recruitment-gacha` MODIFIED: `Resolved spriteKey SHALL support male / female variants` requirement + 4 BDD scenarios (female pick / male pick / fallback to male when female missing / starter pull skips random picker)
   - `hospital-scene` MODIFIED: `Scene asset SHALL switch by hospital tier` extended to 4 tiers + scenarios for tier4 render / tier3→tier4 transition / graceful degradation when tier4 missing

### Chrome MCP smoke (3/3 passed)

- **Smoke A — Tier 4 scene renders**: forced `tier = '國家級教學醫院' + reputation 2.5M` in IDB, reload, verified `<img alt="Hospital scene: 國家級教學醫院" src=".../sprites/scenes/hospital-tier4-national.png" />` rendered. Tier line shows `醫院：國家級教學醫院 ⭐ 已達頂峰`.
- **Smoke B — Female sprite resolution**: force-wrote a doctor with `spriteKey: 'doctor-內科-P3-female'` directly to IDB, navigated to `/#/roster`, verified `<img src=".../sprites/doctor-%E5%85%A7%E7%A7%91-P3-female.png" />` rendered correctly via the SPRITES_MAP → lookupSprite chain. Other doctors fall back to `doctor-default-{rarity}.png` as expected.
- **Smoke C — Retire flow end-to-end**: clicked 退休 on 內科 醫師 #1 (P4), confirmed modal showed `將返還 1,000 💰 + 24小時 grace note`. After confirm: doctors 4→3, revenue 40000→41000 (Δ +1000 = P4 powerMultiplier 1.0 × 1000), retirementLog row inserted with correct shape.

### Visual QA

4 female sprites + tier4 scene opened in macOS Preview during pipeline. All match expected pixel quality: GBA-era 16-color palette, transparent bg, ~5-20KB sprite / 52KB scene. Some intermediate chromakey/source/alpha PNGs codex generated during post-processing left in `/tmp/sprite-batch-2026-05-17/` — ignored (only named outputs moved to package).

### Curator note

`@study-rpg/core` bumped to 0.3.0 locally but **NOT published to npm yet**. The `workspace:^` protocol in consumer package.jsons makes them use the workspace version regardless. When publishing 0.3.0, the workspace dependents can revert to `^0.3.0` semver to test published-consume path.

Today's running total: 7 commits.

# Decisions — 2026-05-15

## 00:15 — Node 20 GitHub Actions deprecation timeline

GitHub 首次 deploy run（[run 25873707221](https://github.com/fireman333/study-rpg/actions/runs/25873707221)）在 annotations 列出 Node 20 actions 的退役日期：

- **2026-06-02**: actions 預設會被 force 到 Node 24
- **2026-09-16**: Node 20 從 runner 完全移除

當前 workflow 用的 `actions/checkout@v4`、`actions/setup-node@v4`、`actions/upload-pages-artifact@v3`、`actions/deploy-pages@v4`、`pnpm/action-setup@v4` 都還在 Node 20 base。

**Action item（給未來的自己）**: 2026-08 之前開 change `bump-actions-to-node24` — 把每個 action 升到 Node 24-compatible 版本（或在 workflow yml 加 `env: { FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true' }` 暫時 opt-in）。可以等 2026-06 GitHub 發新版 actions 時順手升。

不阻塞 M2 工作 — Node 24 force 後當前 actions 仍會 silent migrate 跑得起來，只是不再是「intended runtime」。

## 00:18 — M1 first deploy 確認 live

- URL: https://fireman333.github.io/study-rpg/
- First deploy timestamp: 2026-05-15T17:03:49Z
- Build time: 21s
- Bundle size: 5.74 MB raw / 2.09 MB gzipped
- Chrome MCP smoke: 4 stat / 3291 Q quiz pool / 0 console errors

`project.md` 的 URL 從「暫定」變成「live」，但 project.md 本身已標 M1 ✓ shipped (2026-05-15)，不需要再 patch。

## 00:21 — `gh` CLI 完整自動化首次 deploy 流程可行

不需要手點 GitHub UI，全部走 CLI：

```bash
gh repo create <owner>/<repo> --public --description "..." --source . --remote origin --push
gh api repos/<owner>/<repo>/pages -X POST -f build_type=workflow
gh api repos/<owner>/<repo>/actions/permissions/workflow -X PUT -F default_workflow_permissions=write -F can_approve_pull_request_reviews=false
gh run watch <run-id> --exit-status
```

未來 fork / 新 project 起 GH Pages 都可以複製這個 pattern。需要 `gh auth scope: repo + workflow`（auth status 可確認）。

## 11:50 — wire-hospital-tycoon-engine grill-vs-implementation reconciliation

`/spec resume` session（cwd main worktree）做完 `add-doctor-sprite-roster` 收尾後，跑 `/grill quick software-project` 規劃 `wire-hospital-tycoon-engine`。grill 同時，平行 session 在 m2 worktree 已完成 implementation + archive（commits `e069348` / `28dbb1d` / `7bac7df`，11:16）。Grill summary 留在 `~/.claude/scratch/grilled-wire-hospital-tycoon-engine-2026-05-15.md` 變成 retroactive 分析。

**選了 R3 reconciliation path**（accept reality + forward deferred items into next change），原因：implementation 已 archive + Chrome MCP smoke ✓，撤回成本 > 補做價值。

**Diverge map（grill 想做 vs 實作現況）**：

| 維度 | Grill 結論 | 實作現況 | 處理 |
|---|---|---|---|
| Tick 模式 | Hybrid（per-Q + idle） | 純 real-time 5s tick | per-Q hook **carry forward 進 `wire-hospital-reputation`** scope |
| Doctor affinity bonus | 1.5× when subject↔room match | 完全沒實作 | **carry forward 進 `wire-hospital-reputation`**（throughput 公式本來就要重塑） |
| Currency | 單一聲望 | Dual `revenue` + `reputation`（1:1） | **保留 dual**：revenue 留著當未來 `wire-hospital-spend` 的 sink；目前 1:1 同步無 UX 混淆，spend mechanic 出來再分化 |
| Reputation shape | Diminishing (log/sqrt) | Linear `delta = throughput × elapsed` | `wire-hospital-reputation` lock 最終 shape |
| Numbers lock | Three-stage rollout | 診所 baseline 已鎖（baseRate=10 / facility=1.0 / 5s tick / 5min offline cap） | 算「Initial-guess provisional」— spec 內部標 `wire-clinic-level-up will extend`，符合 dogfood-revisit 紀律 |

**對下一個 change `wire-hospital-reputation` propose 時的 forward notes**（給未來自己的提醒）：

1. **Subject↔room mapping table**（grill 沒鎖、需在 propose 時 grill）— 候選對應：
   - 內科 → ward
   - 外科 / 骨科 / 婦產（手術部分）/ 耳鼻喉 / 眼科 / 泌尿 → surgery
   - 家醫 / 小兒 / 皮膚 / 精神 / 復健 / 神經內科 / 麻醉 → outpatient
   - 是否多 room 配對（內科 ward + outpatient 都拿 bonus）也要決定
2. **Per-question reputation hook**：除 5s tick 外，每答對一題 給定量 reputation（依當前 assigned doctors 的瞬時 throughput）。需設計 reputation/answer 跟 reputation/tick 兩者比例權重，避免 hybrid 模式 imbalance
3. **Reputation formula shape**：log vs sqrt 兩者選一；formula constants k / log base 在此 change lock
4. **Revenue vs reputation 分化**：本 change 是否讓 revenue ≠ reputation（e.g. reputation 加 log curve、revenue 維持 linear，分別 feed 不同 mechanic）— 等 `wire-hospital-spend` propose 時再決，本 change 仍保 1:1 也 OK
5. **Tick interval 調整**：5s 已 dogfood；如果太頻繁可改 10s 或 15s。但要在此 change spec 中明示「revisited based on dogfood」

## 12:35 — Session 紀律：one session = one track（一階 OR 二階）

本 session 從 main worktree 起（一階 track），但 `/spec resume` 階段檢測到 m2 worktree 有未收尾 change → 我直接開始做 二階 work（add-doctor-sprite-roster archive、wire-hospital-tycoon-engine reconciliation、wire-hospital-reputation grill）整整 2 小時，途中還跟 m2 parallel session 撞了 2 次（tycoon-engine 自我實作 + clinic-level-up 進行中）。

**教訓**：`project.md` 已寫「一階 session: `cd ~/coding-scratch/study-rpg/`、二階 session: `cd ~/coding-scratch/study-rpg-m2/`」但**沒有 session-discipline rule**。下次 `/spec resume` 若 cwd 在 main 但只有 m2 有 active work，應該提示「這要切到 二階 session 跑」而不是直接做下去。

**Next session 方向**（使用者明示）：**一階 M5 模擬考全套**（80Q / 計時 / 出歷年百分位）— 對 2026 H2 一階國考最有直接 utility。流程候選：
1. `/grill quick` 模擬考的 5-7 個 design 問題（題目隨機 vs 整套 / 計時模式 / 中途暫停 / 出分後是否解析 / 百分位資料來源）
2. `/opsx:propose add-mock-exam-mode`（直接 propose）

cwd 必須在 `~/coding-scratch/study-rpg/`（main，一階）— **不要再混進 m2 work**。本日 二階 paused 的 grill 結果存在 `~/.claude/scratch/grilled-wire-hospital-reputation-2026-05-15.md`，下次 二階 session（cd m2 worktree）會自動透過 scratch retrieve。

## 15:06 — M_2nd ✓ shipped: 二階 live at `/study-rpg/hospital/`

M_2nd 8 個 planned changes 全部 archived（hospital-mode-scaffold / medexam2-corpus-ingestion / doctor-sprite-roster / recruitment-gacha / hospital-tycoon-engine / clinic-level-up / hospital-reputation / medexam2-deploy-subpath），且最後一個 `wire-medexam2-deploy-subpath` 今天透過 fast-forward merge `track-m2` → `main` 並 push 觸發 GH Actions run [25905057219](https://github.com/fireman333/study-rpg/actions/runs/25905057219)（32s 全綠）後正式 live。

**Live URLs**:
- 一階: https://fireman333.github.io/study-rpg/
- 二階: https://fireman333.github.io/study-rpg/hospital/

**Chrome MCP prod verify**（Group 11，今天 session 跑完）：
- 一階 SPA 三件套 ✓（home / /skills direct URL / F5 on /skills）— BrowserRouter + rafgraph 404.html fallback 正常
- 二階 SPA 三件套 ✓（/hospital/ home / `#/roster` direct hash / F5 on /roster）— HashRouter 直接 serve `index.html`，無需 404 fallback
- 二階 in-app nav ✓（home ↔ roster click 雙向）
- 0 console errors

**Subpath co-location convention locked**: 同 repo 兩 app 共用單一 GH Pages site `https://fireman333.github.io/study-rpg/`，二階 deploy 到 site subpath `/hospital/`。未來如果加第三個 mode（例如 PGY rotation tycoon、學測 RPG），沿用 `/study-rpg/<mode>/` pattern — 不開 sister repo。Vite base path 跟 deploy.yml subpath merge step 已 lock 在 `wire-medexam2-deploy-subpath` archive 的 spec/deploy-pipeline/spec.md。

**Chrome MCP quirk update**: 上個 session 的 handoff 警告 Chrome MCP 對 fireman333.github.io 有 ERR_CONNECTION_RESET（QUIC/HSTS 殘留）— 但本 session 跑 prod verify 時**完全沒撞到 quirk**。可能是 transient browser cache 問題（refresh / reconnect 自然解），不是 deploy 問題。下次若再 hit，先試開新 tab / Chrome MCP 重連，再 fallback curl-based smoke。

**Node 20 deprecation**（也記在 00:15 entry）持續 pending：CI run 仍 emit annotation，但不阻塞 deploy；2026-08 之前另開 `bump-actions-to-node24` change 一次升完。

**M_2nd 結束、下一步 M3** = `@study-rpg/core` 發 npm（0.1.x），讓真正的第三方 fork 也能用同一 engine。M_2nd 算 owner-internal fork dogfood，公開 API contract 由 M3 lock。

## 15:50 — Next-session pointer: sprite v2 fix for cosmetic layers

M5 已 ship（commit `8c6ff8a`），但 cosmetic sprite 視覺品質有 2 個 known issue：

**P1 — 醫學生眼鏡 oversized**（Chrome MCP smoke 顯示）：codex 生 v1 時把眼鏡放滿 384×384 canvas 中央，overlay 到 character-base 上時遮住整張臉。已生 v2（嚴格 prompt：「眼鏡只占 15% × 8% area at upper-center coordinates」），stashed 在 `~/.claude/scratch/sprite-v2-2026-05-15/cosmetic-head-medical-student-glasses-v2.png` (2.9 KB)。

**P2 — detailed-notes bundle bloat**（235 KB vs 平均 30 KB）：codex 未正確 quantize 到 16-color palette。已生 v2 with strict palette enforcement，stashed 在 `~/.claude/scratch/sprite-v2-2026-05-15/cosmetic-held-detailed-notes-v2.png` (5.5 KB — 42× smaller)。

**Next-session 計畫（`/spec resume` 後）**：
1. **檢視 v2 視覺品質**：`open ~/.claude/scratch/sprite-v2-2026-05-15/*.png` 跟現役對比
2. **若 v2 OK** → 直接 `mv ~/.claude/scratch/sprite-v2-2026-05-15/*.png packages/theme-pixel-medical/sprites/` overwrite，然後 build + Chrome MCP smoke 確認，commit
3. **若 v2 還是不夠好** → 改 sprite 策略（CSS scaling per layer category，例 `dorm-layer-head { width: 30%; top: 12%; left: 35% }` 統一縮所有 head sprite）
4. **可選：批次 regen 剩下 14 sprite**（4 body coats + 4 accessories + 3 held + 4 head 中除眼鏡的 3 個）— 用 v2 同一 prompt template 但對應各 category 的 position/size。Sprite gen ~15-20 min wall 並行 batch 4

**避免重做的脈絡**：
- v1 prompt 跟 v2 prompt 的差異是 "CRITICAL CONSTRAINTS" + 精確 % 區域 + 大量 transparency 強調。v2 在 sprite v1 的 issue 上是「強制要求小尺寸 + 大透明」。沒這個強調 codex 會把主題畫滿 canvas
- character-base sprite 不是 transparent — 有自帶背景。所以 dorm-default 背景在 character 後面看不到（被 character 自帶背景蓋掉）。要 fix 需要做 v2 character sprite（transparent bg）— 不在本 next-session scope，太大改動

**Out of scope（不做）**：
- character-base 重生（會破壞 home view CharCard 視覺）
- Background sprite v2（4 個全 canvas，無 alignment issue）
- Body coat v2（穿身上比例感覺對，未 smoke test 但 dogfood 再說）

**驗證標準**（apply v2 後）：
- detailed-notes 大小 < 50 KB
- 醫學生眼鏡套上 character 後，眼鏡視覺寬度約等於眼睛區域（不蓋滿臉）
- Chrome MCP smoke：dorm → equip 醫學生眼鏡 → screenshot 對比 v1

## 16:05 — add-hospital-home-pixel-scene archived (二階 home scene live)

二階 home (`/study-rpg/hospital/#/`) 加入 tier-based pixel scene + subject-bound doctor slots + UpgradeModal。完整 grill (Quick 5 min) → propose → apply → deploy → archive 一條龍今天跑完。

**Archive**: `openspec/changes/archive/2026-05-15-add-hospital-home-pixel-scene/` (commit `2e56c07` feat + `63c27cb` merge main into track-m2)

**Spec deltas synced**:
- New capability `hospital-scene` (7 reqs) — render position / tier asset swap / subject-bound doctor slots / building click → upgrade modal / responsive layout / `?scene=off` flag / graceful asset fail
- `theme-pack-contract` +1 req (8 → 9) — optional `scenes` + `doctorSlotPositions` fields (non-breaking)

**Live URLs (post-deploy run [25907271662](https://github.com/fireman333/study-rpg/actions/runs/25907271662))**:
- 一階 home: https://fireman333.github.io/study-rpg/ (含 add-cosmetic-and-dorm M5 work merged in)
- 二階 home: https://fireman333.github.io/study-rpg/hospital/ (含 hospital scene)

**Chrome MCP prod verify**：scene render ✓、tier1 PNG load ✓、click → upgrade modal ✓、`?scene=off` fallback ✓、hash route /roster ✓、0 console errors

**Codex asset gen 配方 update**：codex CLI 在 `/tmp` 被 git trust guard 擋 — 解法是加 `--skip-git-repo-check` flag。`~/.claude/imports/codex_image_gen.md` 已有此 flag 的 fail 案例但沒寫進 minimal config，下次寫 image gen pipeline 時要直接套：

```bash
cd /tmp && codex exec --sandbox workspace-write --skip-git-repo-check \
  '<prompt> ... Save to /tmp/<name>.png. $imagegen' < /dev/null
```

**Dogfood telemetry 觀察點**（接下來幾天玩二階 hospital mode 時記下來）：
- Scene asset 視覺風格 — 3 tier 並置看起來「同一風格」嗎？光線、配色、視角一致？不一致就 re-gen
- Doctor slot 座標 — 96×96 sprite 落在 tier 2/3 scene 上會不會卡到建築 / 互相重疊？需要調 x/y
- Mobile 表現（< 480 px）— doctor sprite 12.5% 寬可能太小、難辨識；可能需要加 `?scene=building-only` mode
- Tier upgrade UX — 點 scene 整張可點是否太敏感？意外觸發 modal 頻率
- Bundle size 影響 — scene PNG +355 KB raw（tier 1 60 / tier 2 138 / tier 3 158 KB）；首屏 loading 感是否仍 < 3s

**Two parallel tracks 合流今天**：原本 main worktree (一階) 跑 `add-cosmetic-and-dorm` + track-m2 worktree (二階) 跑 `add-hospital-home-pixel-scene` 並行。合流靠 3-way merge — types.ts / project.md auto-merged、decisions.md 手動 resolve (兩 entry 都保)。下次平行開發要注意：types.ts ThemePack 是熱點 file，兩 branch 同時改要預期 conflict。

**Next M3 = `@study-rpg/core` npm publish** — 兩 track 合流點，把 engine 公開出去讓真正第三方 fork。M_2nd + M5 都完成，本 session 是 M2 + M_2nd + M5 三 milestone 共同 ship 日。

## 19:50 — wire-hospital-quiz-ui apply complete (心臟接上了)

二階 hospital mode 從純 idle tycoon 升級為真正的 study RPG — quiz UI 接上心臟。`/opsx:apply` 全部 11 task group / 61 tasks 大部分完成；3 個 task 標為 deferred（3.4 Vitest infra 不存在；1.8 + 9.9 dogfood v3→v4 migration runtime simulation 同 tab 內難測；10.4 prod URL smoke 等 deploy 後）。

**Implementation summary**:
- 3 new capability 全部 ADDED、0 MODIFIED — 不動 `recruitment-gacha` / `hospital-management-mode` / 等任何 archived spec
- Dexie v3 → v4 migration：additive 2 table (`mastery`, `questionHistory`) + `gameCounters.hasUsedStarterPull` 新欄位；upgrade hook backfill 14 mastery default + force `hasUsedStarterPull = true`（避免 dogfood 升級看到 starter card）
- 4 new components: `QuizModal` / `StarterPullCard` / `StarterPullModal` / 更新 `RecruitmentBanner` 雙 button
- 3 new lib/service files: `lib/quiz.ts`（picker）/ `lib/mastery.ts`（recordCorrect/Wrong + format）/ `services/starter-pull.ts`（保底 P4+ roll）
- Content pack: `STARTER_PULL_WEIGHTS` 新 export

**Chrome MCP smoke 結論（fresh save scenario, prod build at vite preview:5273）**：
- ✓ 2 P5 starter doctors（內科 + 外科）seeded、14 mastery rows initialized、tickets=10
- ✓ StarterPullCard 顯示、點開後 14 subject chip 模態框、選 婦產科 → P3 人上人（非 P5，distribution 工作正常）、card 用完消失
- ✓ Quiz modal: 點內科 banner 學習 → 開模態、partner = 外科 醫師 #1 (most recent obtainedAt)、subject 預設內科
- ✓ Correct answer: affinity[內科]+=1, mastery 1/1, history row with SRS reserved fields (nextDueAt=null, interval=0, easeFactor=2.5)
- ✓ Wrong answer: mastery total+=1 only, affinity unchanged, explanation rendered from corpus
- ✓ Mastery surface: 內科 banner 顯示「掌握 50%」(1/2), 婦產科「掌握 -」(total=0)
- ✓ Locked banner（眼科）: 學習 enabled、招募 disabled（spec compliant）
- ✓ HashRouter SPA 三件套（in-app nav / `/#/roster` direct URL / F5 on hash route）— prod build 無 404 風險（design Decision 10 已預期）

**Carry forward / 待 archive 後做**:
- `wire-hospital-srs-queue` — 下個 change scheduler：讀本 change 預留的 history.nextDueAt / interval / easeFactor 跑 SM-2 風格 schedule、surface due UI
- 真正的 v3→v4 migration 會在 user 開 prod 第一次跑時 exercise；migration logic（schema.ts version(4).upgrade + ensureSeed force flag）已 review、邏輯 additive idempotent、不破壞既有資料
- Specialty match quiz-time bonus（doctor.subject ≡ quiz.subject 加倍）— dogfood 觀察後 propose follow-up
- Quiz subject dropdown 切換 reset session — 已實作（reset seenIds + 載新題、不動 mastery / affinity）

**心臟接上後 二階 完整 game loop**：
1. 新 save → 拿 2 P5 starter + 首抽 P4+ → 開始有 3 個 doctor
2. 點 banner 學習 → 答題 → affinity[X] +=1 + mastery 累積
3. Affinity 達 threshold → banner unlock → 用 ticket 招募 → 抽到隨機 rarity 新 doctor
4. 指派 doctor 到 room → 5s tick 累 reputation + revenue → tier upgrade
5. 持續 study + recruit + manage 循環

**Deferred polish（user explicit「要修但下次做」）**：
- **Quiz modal partner section 套 rarity color**：目前 `.quiz-modal__partner` 沒 inline `--rarity-color: var(--rarity-${doctor.rarity.toLowerCase()})`，P1 跟 P5 partner 視覺除了 sprite + label 沒邊框色差異。修法：QuizModal partner 包一層 div、套 inline style，再給 `.quiz-modal__partner` 加 `border-left: 4px solid var(--rarity-color)` 之類視覺指示。**Trigger 點**：下個 sprite v2 polish 輪或 dogfood 抱怨「partner 看起來都一樣」時做。Cross-ref：`openspec/changes/archive/2026-05-15-wire-hospital-quiz-ui/design.md` Open Q6（同步補）。

## 21:35 — wire-hospital-srs-queue apply complete (SRS scheduler 接上)

二階 hospital mode 的 SM-2 spaced repetition scheduler 接上 `questionHistory` v4 預留欄位。

**Implementation**:
- **Core** `packages/core/src/lib/srs.ts`: 新增 `reviewCardBinary({ correct, prev, now? })` 二階 binary 變體 + 4 個 named-export tunable constants (`WRONG_INTERVAL_MULTIPLIER=0.5` / `WRONG_EASE_MULTIPLIER=0.85` / `STANDARD_INITIAL_INTERVALS=[1,6]` / `SRS_DAILY_CAP=20`)。一階 `reviewCard(quality 0..5)` 不動，新舊共存
- **二階 service** `apps/medexam2-hospital-tw/src/lib/srs-scheduler.ts`: `getDueQueueAllSubjects` + `allocateDailyCap`（round-robin 跨 subject）+ `getNextDueCardForSubject`。`isSrsDisabled()` helper 看 `?srs=off` URL flag 給 dogfood A/B
- **Mastery hook**: `lib/mastery.ts` `upsertHistory` 內呼叫 `reviewCardBinary` 算 SRS 寫回（同一 Dexie transaction，atomicity 保留）
- **UI**: `RecruitmentBanner` 加 due chip `🔴 N`（cap 後 per-subject count，0 不顯示、>99 顯示 99+），透過 HomePage `useLiveQuery` 自動 reactive
- **Quiz modal due-first**: `QuizModal.loadNextQuestion` 先試 `getNextDueCardForSubject(consumedDueIds)`，命中走 `pickQuestionById`（新加在 `lib/quiz.ts`），未命中或 orphan 才 fallback `pickRandomQuestion`；due 卡不入 `seenIds`

**Spec scenario coverage**（Chrome MCP smoke 全綠）:
| Req | Scenarios covered |
|---|---|
| 1 Binary SM-2 review | First correct (Q30: i=0→1, EF=2.5) ✓; Second correct (Q31 post-recovery: i=1→6) ✓ |
| 2 Partial reset on wrong | Q31 first wrong: i=0→1, EF=2.5×0.85=2.125 ✓ |
| 3 Banner badge surface | 2 due rows → 內科 chip `🔴 2`; answer 1 → chip `🔴 1` ✓ |
| 4 Due-first picker | Click 內科 學習 after time-travel → modal opens with overdue Q31 ✓ |
| 5 Daily cap round-robin | 14 subjects each with N due → tested via spec, code reviewed |
| 6 SRS independence | Mastery=1/2 correct, affinity=1 — SRS 用獨立欄位 ✓ |
| 7 No schema migration | typecheck + v4 read against existing schema ✓ |

**Code touch summary**:
- 4 modified: `packages/core/src/lib/srs.ts`、`packages/core/src/index.ts`、`apps/medexam2-hospital-tw/src/lib/mastery.ts`、`apps/medexam2-hospital-tw/src/lib/quiz.ts`、`apps/medexam2-hospital-tw/src/pages/HomePage.tsx`、`apps/medexam2-hospital-tw/src/components/RecruitmentBanner.tsx`、`apps/medexam2-hospital-tw/src/components/QuizModal.tsx`、`apps/medexam2-hospital-tw/src/styles.css`
- 1 new: `apps/medexam2-hospital-tw/src/lib/srs-scheduler.ts`
- 0 schema migration（v4 預留欄位夠用）

**Tunable constants（dogfood-driven）** 全部集中在 `packages/core/src/lib/srs.ts` 頂部。未來如 dogfood 顯示 daily cap 20 太多/少、partial reset 太溫和/嚴厲 → 改該檔常數即可，無需 spec 變動。

**Carry-forward next change `wire-hospital-specialty-bonus`**:
- Doctor `subject` ≡ quiz `subject` 時 mastery / affinity reward × 1.5（grilled 已記在 wire-hospital-quiz-ui handoff）
- SRS interval / EF 不受 specialty match 影響（本 change spec 已明示 Req 6）
- 建議走 grill quick → propose / apply / archive 標準流程，spec touch: 新 capability `hospital-specialty-bonus` 或 MODIFY `hospital-quiz` requirements

**Dogfood telemetry 觀察點**（用戶接下來幾天玩二階時記下來）:
- Daily cap 20 體感是否合適（每天 due > 20 / day 算 cap kick in；< 20 表 cap 沒實際限制作用）
- Partial reset 0.5/0.85 是否讓 user 仍有「壓力」答對（vs Anki harsh 反饋）— 若太鬆 → 0.4/0.8；若太緊 → 0.6/0.9
- Banner chip 紅色配色（`#d9534f`）跟整體 design system 是否衝突 — 不衝突再 lock；衝突改成 `--accent-rose` token
- Specialty match deferred — dogfood 時觀察「想要同科 doctor 配對加成」需求強度

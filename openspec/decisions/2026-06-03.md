# Decisions — 2026-06-03

## split-medexam2-standalone — shipped + DECISION: keep monorepo 二階 source (defer deletion)

`split-medexam2-standalone` is ✓ Complete, live in prod, and archived
(`archive/2026-06-03-split-medexam2-standalone/`). The split **decoupled 二階's
deploy** from the monorepo; it did **NOT** delete the 二階 source.

**Decision (owner, 2026-06-03): KEEP the dormant 二階 source in the `study-rpg`
monorepo for now — do NOT delete it as a standalone step.** Fold the deletion into
`remove-medexam-tw-and-promote-neurons` (which already tears down workspace +
pipelines), so we don't pay a separate deploy/workspace-churn change.

**Current truth about 二階 in the monorepo:**
- Deploy pipeline: ✅ 二階 fully removed (combined CF `build:cf` + `build-cf-pages-dist.mjs` ROUTES + `deploy.yml` + `deploy-cf-pages.yml`; remaining `medexam2`/二階 strings are explanatory comments only).
- Source: ❌ still physically present — `apps/medexam2-hospital-tw/` (151 src files), `packages/theme-pixel-hospital/` (7), `packages/content-medexam2-tw/` (14); still in the pnpm workspace (`dev:m2` / `build:m2` aliases kept; `pnpm -r build`/`typecheck` still touch it).
- ⚠️ **Two copies now exist.** Canonical / live = the standalone repo `study-rpg-2nd` (served at `med-study-rpg.com/2nd/` via edge-router). The monorepo copy is **dead/dormant** and **will drift**. **All future 二階 edits go to `study-rpg-2nd`, never the monorepo copy.**

## TODO — outstanding work (consolidated)

### A. split-medexam2-standalone tail (small)
- [ ] **A1** — Merge the archive to main: track-m2 commits `e89ec07` (post-merge verify) + `355af59` (archive + synced specs `medexam2-standalone` ADDED / `deploy-pipeline` 16→15) are on `origin/track-m2` only. They ride to `main` with the **next deploy-affecting change** (deliberately not merged solo — `deploy.yml`/`deploy-cf-pages.yml` fire on any main push, so avoid a doc-only redeploy). Until then `main` openspec is "applied-but-not-archived" — expected.
- [ ] **A2** — `study-rpg-2nd` repo README (separate repo): document the npm-`@study-rpg/core` publish→bump loop (edit core in original repo → bump + `pnpm --filter @study-rpg/core publish` from owner's terminal w/ npm 2FA → bump dep + `pnpm install` in study-rpg-2nd).
- [ ] **A3** — (bake-end) From Supabase Auth allowlist + `docs/AUTH_REDIRECT_URIS.md`, the `https://fireman333.github.io/study-rpg/hospital/**` entry is now MOOT (that path is 301-only). Remove it alongside the other GH-Pages entries when the domain-migration bake ends.

### B. Next big changes (drafts already in main worktree, NOT started)
- [ ] **B1 — GREENLIT (owner, 2026-06-03)** — `remove-medexam-tw-and-promote-neurons` (draft: proposal only, `~/coding-scratch/study-rpg/openspec/changes/`). **KEY FACT: 一階 (medexam-tw) has NO real players** (owner confirmed) — so the BREAKING data-loss bits are low-stakes: dropping the 4 一階 Supabase tables' rows, `/1st/` → 404, deleting R2 `m1` blobs all touch only dogfood/owner data, nothing to preserve. BREAKING teardown: delete 一階 app + `content-medexam-tw` + `theme-pixel-medical`; drop 4 Supabase tables + 32 RLS; delete R2 `m1` blobs; `/1st/` → 404; remove 一階 from both deploy pipelines; **promote neurons to canonical**. **Scope confirmed to also fold in: delete the dormant monorepo 二階 source** (`apps/medexam2-hospital-tw` + `theme-pixel-hospital` + `content-medexam2-tw`) per the KEEP-then-fold decision above. Run in the **main worktree** (`~/coding-scratch/study-rpg`), NOT track-m2. Two open decisions in the draft to resolve first (GH Pages root URL fate; root landing-page mechanism) → recommend `/grill` then `/opsx:propose` (draft is proposal-only; needs design + tasks + spec deltas fleshed out).
- [ ] **B2** — `add-cloudflare-auth-migration` (draft: proposal + design + tasks + specs, fairly complete). Move Auth + `bug_reports` + 3 RPCs (`delete_my_data`/`delete_my_account`/`export_my_data`) off Supabase → Worker-hosted Google OAuth + ES256 JWT + D1. Spans all 3 apps; forced re-login for ~29 dogfood users; identity preserved via Google `sub` → seeded UUID. Run in **main worktree**.

### C. Pre-existing in-progress changes (stalled — triage separately)
- [x] **C1 — RESOLVED: ABANDONED** (owner, 2026-06-03). `add-hospital-equipment-medexam2` was a 二階 change orphaned by the split. Tombstoned to `openspec/changes/archive/2026-06-03-abandoned-add-hospital-equipment-medexam2/` (+ `ABANDONED.md`); delta specs (`clinic-level-up` MODIFIED / `hospital-equipment` ADDED) **NOT synced** to main specs. Feature code (§1-8,10-11) rode along to `study-rpg-2nd` at the split; §9 R2 sync never implemented (now new repo's concern if needed). project.md Roadmap row marked ABANDONED.
- [ ] **C2** — `add-r2-cloud-sync-migration` (71/85). Phase 4 baking; mostly done.
- [ ] **C3** — `add-version-check-banner` (0/22). Not started.

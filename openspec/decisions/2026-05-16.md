# Decisions — 2026-05-16

## 23:45 — wire-hospital-specialty-bonus apply complete (同科加成接上)

二階 hospital mode quiz pipeline 接上 specialty match bonus — partner doctor 同科 quiz 時 mastery 累積套 rarity-tiered multiplier (P1=1.5 / P5=1.05)，bundled deferred polish（partner section rarity color border-left）一起完成。

**Implementation**:
- **Content pack** `packages/content-medexam2-tw/src/specialty.ts` (new): `SPECIALTY_MATCH_MULTIPLIER` 表 (Object.freeze) + `getSpecialtyMultiplier()` pure helper。Direction 沿用既有 `AFFINITY_MATCH_BONUS`（rare doctor 加成最強，P1 夯 = 1.5× / P5 拉完了 = 1.05×、cross-subject = 1.0×）
- **Mastery hook** `lib/mastery.ts`: `recordCorrectAnswer` 加 optional `partner: { subjectId, rarity } | null` 第二參數，內呼 `getSpecialtyMultiplier()`、傳給 `upsertMastery(..., multiplier)`。`recordWrongAnswer` 完全不動（multiplier 永不套錯題，per spec Req 2 scenario）
- **UI** `QuizModal.tsx`: 計算 `specialtyMultiplier = getSpecialtyMultiplier(boundDoctor?.subjectId, boundDoctor?.rarity, subjectId)` per render，> 1.0 才 render `✨ {n}×` chip；partner section 加 inline `borderLeft: 4px solid var(--rarity-${rarity})`。Dropdown 切換 partner / subject 都會 reactively 更新（透過既有 React state，無新 hook）
- **CSS** `styles.css`: 新 `.quiz-modal__partner-bonus` (gold-tone rounded chip)；既有 partner section `border: 2px dashed` 跟新 inline `border-left` 透過 inline 優先級自然 stack（dashed 三邊 + solid rarity 左邊）

**Scope confirm**:
- 1 new capability: `hospital-specialty-bonus` (7 reqs / 14 scenarios)
- 0 modified capabilities (hospital-quiz / hospital-mastery / hospital-srs / recruitment-gacha 全 archive 不動)
- Code touch: 5 files (1 new + 4 modified)
- 0 schema migration (mastery.correct 已是 JS number = float native)
- `@study-rpg/core@0.1.0` API surface 不擴張（specialty match 是 content-pack-level concept）

**Spec scenario coverage**（Chrome MCP smoke 全綠）:
| Req | Scenarios covered |
|---|---|
| 1 Specialty match predicate | exact match true / cross-subject false / no partner false — 透過 (6.3) (6.4) (6.6) 三 case |
| 2 Tier-based mastery multiplier | P5 same-subject `correct=1.05` (6.3) + P1 same-subject `correct=1.5` (6.7) + cross-subject `correct=1.0` (6.4) + wrong answer never applies (carry-forward from wire-hospital-quiz-ui smoke) |
| 3 Mastery-only application scope | affinity 仍 +1 / SRS interval=1 EF=2.5 standard 不受 multiplier 影響 (6.3 結果) |
| 4 Float storage no migration | mastery.correct 3.55 / 2.05 / 1.05 都 in-place 寫 v4 schema (6.7) |
| 5 Partner specialty chip | P5 → `✨ 1.05×` chip / P1 → `✨ 1.5×` chip / cross-subject → no chip (6.5 6.6 6.7) |
| 6 Partner section rarity border | P5 border `rgb(216, 216, 216)` / P1 border `rgb(196, 77, 77)` (6.5 6.7) |
| 7 Tunable constants in content-medexam2-tw | typecheck 確認 import path 正確、core 公開 API 不變 |

**Tunable constants location**: `packages/content-medexam2-tw/src/specialty.ts` 頂部 `SPECIALTY_MATCH_MULTIPLIER`。未來 dogfood 後想 tune（e.g. P5 從 1.05 → 1.1 給拉完了更多 boost、或 P1 從 1.5 → 2.0 給夯更猛）→ 改該檔 5 個 literal、follow-up change archive 即可。

**Mastery > 100% display**:
- 設計選擇：UI 不 cap，直接顯示「掌握 118%」/「掌握 150%」等 game-y feel
- Chrome MCP confirm: P1 P5 cross 三題後 `掌握 118%` 顯示正常、無 layout overflow
- Dogfood 觀察點：user 看到 > 100% 心理反應是「我學超過了」（game-y）還是「mastery 怎麼會 > 100%」（confused）— follow-up change 可改 cap + bonus 顯示（per design.md Risk 2 mitigation）

**Carry-forward 收掉**: `wire-hospital-quiz-ui` decisions 21:35 deferred「Quiz modal partner section 套 rarity color」polish 在本 change bundled 完成 — `border-left: 4px solid var(--rarity-${rarity})` 透過 inline style，加上既有 `border: 2px dashed var(--frame-dark)` 自然 stack 出「dashed 三邊 + solid rarity 左邊」視覺。

**Tasks gated for user**:
- 8.3 `/opsx:archive`（needs explicit user confirm per Curator rule）
- 8.4 auto-git commit（needs explicit user confirm per Curator rule）

**Next follow-up candidates**:
- `extend-specialty-cluster-match` — cluster-aware match (e.g. 內科 cluster: 內科 / 神經內科 / 家醫科; 0.5× partial bonus for cluster mate)
- `wire-affinity-specialty-bonus` — affinity 也加 multiplier（目前只 mastery）
- `polish-mastery-percent-cap` — 若 dogfood 反饋 > 100% UI 困惑，改 cap 100% + 顯示 `(+N bonus)`

## 12:36 — wire-affinity-specialty-bonus apply complete (affinity 同步套 specialty multiplier)

把上一個 change `wire-hospital-specialty-bonus` deferred 的 carry-forward 「affinity 也加 multiplier」一條龍 propose → apply → smoke → verify。同一 transaction 內把 mastery 跟 affinity 對 specialty match 的不對稱補上。

**Implementation**（極小 surgical diff：2 files、4 lines net）:
- `apps/medexam2-hospital-tw/src/lib/mastery.ts` line 99：`correctCount + 1` → `correctCount + multiplier`（復用既有 `multiplier` 變數、零新 import、零新 helper、零新 export）
- `apps/medexam2-hospital-tw/src/lib/mastery.ts` jsdoc line 75-81：「Affinity ... unaffected」→「Both mastery.correct and affinity.correctCount deltas are multiplied」
- `apps/medexam2-hospital-tw/src/components/RecruitmentBanner.tsx` line 30：新增 `affinityDisplay = Math.round(affinity * 10) / 10`、line 45 `{affinity}` → `{affinityDisplay}`
- `apps/medexam2-hospital-tw/src/components/RecruitmentBanner.tsx` line 28：`missing = Math.max(0, threshold - affinity)` → `Math.max(0, Math.ceil(threshold - affinity))`（**bundled in-flight UX fix**，避免 「再答對 6.5 題」這種小數 question count）

**Scope confirm**:
- 1 new capability: `affinity-specialty-bonus` (6 reqs / 14 scenarios)
- 0 modified capabilities（`hospital-specialty-bonus` Req 3「Mastery-only application scope」被本 change 隱含 superseded、但 archive 不可動，新 spec 與舊 spec 並存代表「mastery + affinity 都套 multiplier」為最終真值）
- 0 schema migration（affinity.correctCount 已是 JS native double）
- 0 threshold tuning（66 / 15 / 58 整數不動）
- 0 wrong-answer 行為變更（`recordWrongAnswer` 純不動）
- 0 npm publish requirement（`@study-rpg/core@0.2.0` API surface 不擴張）

**Spec scenario coverage**（Chrome MCP smoke via Vite dev module import + Dexie liveQuery UI assertion）:
| Req | Scenarios covered |
|---|---|
| 1 Source of truth reuse | mastery.ts diff: zero new exports；smoke 4.1 用 P1=1.5 confirm 跟 mastery 同 table |
| 2 Tier-based affinity multiplier | 4.1 P1 同科 +1.5 ✓ / 4.2 P5 同科 +1.05 ✓ / 4.3 cross-subject +1.0 ✓ / 4.4 no-partner +1.0 ✓ |
| 3 Wrong answer no affinity change | 4.5 recordWrongAnswer affinity 7.55 → 7.55 (delta 0) ✓ |
| 4 Float in-place no migration | 4.1-4.2 IndexedDB 寫入 4.5 / 5.55 浮點值、無 schema bump ✓ |
| 5 Threshold comparison unchanged | 4.7 14.5 < 15 → locked / 15.0001 ≥ 15 → unlocked ✓ |
| 6 Display smart decimal | 4.7 9.85 → "9.9" / 14.5 → "14.5" / 15.0001 → "15" / 內科 7.55 → "7.6" / 整數無 .0 ✓ |

**Bundled in-flight UX fix (Math.ceil(missing))**:
Apply 階段讀 RecruitmentBanner.tsx 時發現 `missing = threshold - affinity` 在 lines 72, 80（"再答對 X 題", roll button title）會渲染浮點數，user 看到「再答對 58.45 題」UX 壞。Spec Req 6 只規範 `{affinity}` 顯示精度、未涵蓋 `missing` 渲染。**修法**：`missing = Math.ceil(threshold - affinity)`（保守上整、永遠顯示 user 達成 unlock 的最小整數 quiz 次數，與 partner rarity 無關）。Verified smoke 4.7 證實 內科 7.55 / 66 → "再答對 59 題" ✓（59 = ceil(66 - 7.55) = ceil(58.45)）。屬於「Surgical Changes」灰色地帶但同 component / 同渲染路徑、與 spec 主訴一致（rounding for sane display），bundled in 並寫入此 decisions entry 留 audit trail。Spec 文字不追溯修改（archive 不可動）。

**Tunable constants location**: `packages/content-medexam2-tw/src/specialty.ts` 的 `SPECIALTY_MATCH_MULTIPLIER` 表 — **與 mastery 共用**（單一 source of truth、dogfood tuning 只動一個檔）。未來若需 split mastery vs affinity multiplier，獨立 change 加 `getAffinitySpecialtyMultiplier` helper + 改 mastery.ts 兩處即可。

**Gameplay 影響預估**（待 dogfood 驗）:
- P1 同科 partner 連續答對：affinity 從 +10 達標變 +6.67 達標 → banner unlock 快 ~33%
- P5 同科 partner：~5% 加速
- Cross-subject / no-partner：完全不變
- 是否需要 threshold 重新校準：本 change 不動，dogfood 後跟 mastery > 100% UX 議題一起評估

**Tasks gated for user**:
- 7.1 `/opsx:archive`（needs explicit user confirm per Curator rule）
- 7.2 auto-git commit（needs explicit user confirm per Curator rule）
- 7.3 **不**馬上 merge track-m2 → main（dogfood 後再評估）

**Next follow-up candidates**（不變、與上一條同）:
- `extend-specialty-cluster-match` — 內科 cluster partial bonus
- `wire-affinity-specialty-bonus-tune-thresholds` — 若 dogfood 反饋 unlock 太快
- `polish-mastery-percent-cap` — mastery > 100% UI cap
- `wire-xp-specialty-bonus` — XP rewards 也套 multiplier（最後一個 specialty-touchpoint）

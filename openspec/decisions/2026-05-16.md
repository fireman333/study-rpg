# Decisions — 2026-05-16

## 03:26 — M3 milestone: `@study-rpg/core@0.1.0` published to npm

Engine 第一次公開上架。Registry 確認 (search index): publisher `tony85314`, license `AGPL-3.0-or-later`, date `2026-05-16T03:26:29.062Z`, homepage / repository / bugs links 全正確。

**Publish path 實況**:
- 原 caveat 預期 `@study-rpg` org 不存在會撞 403（decisions/2026-05-15.md 16:25 / 20:30 entries）
- 第一次嘗試 publish: 確實噴 `404 Scope not found`
- Owner 選擇先建 `fireman333` free org（誤判：以為要用個人 scope）→ 仍噴同樣 404（因 publish 路徑仍指 `@study-rpg/core`）
- Owner 第二次走正解：去 https://www.npmjs.com/org/create 再建一個 free org `study-rpg` → publish 一發就過
- **Lesson**：兩 org 並存無衝突，未來個人 side project 走 `@fireman333/*`、study-rpg ecosystem 走 `@study-rpg/*`。spec.md 跟 ~90 file 的 `@study-rpg/core` 引用全部不動，省下 mass rename

**Git tag**: `v0.1.0-core` → 26eed29 (`docs(decisions): 2026-05-15 20:30 — reset-cosmetic-paper-doll-fixed-grid archived`) pushed to origin。Tag 落在當前 HEAD 因為 prep-core-npm-publish 已 archive 後沒再 commit code，是正確 release point。

**安全事件 — `~/.npmrc` plaintext auth token 入 LLM context**:
- `npm login` 把 `_authToken=npm_Qbr…kpg` (classic automation token) 寫進 `~/.npmrc`
- Session 中 `cat ~/.npmrc` 撈出來 → 進 LLM API context → 在對話歷史中可追溯
- **Action item（owner 立即跑）**: https://www.npmjs.com/settings/~/tokens 撤掉這顆 classic token，改申請 granular access token（限 `@study-rpg` scope + 限 publish + 加 expiry）。再跑 `npm login` 重新寫一顆乾淨的進 `.npmrc`
- **Lesson for future publish runs**: 跑 `npm publish` 前避免 `cat ~/.npmrc`；用 `npm whoami` / `npm token list` 確認 auth state 即可，不要 dump token 內容

**M3 進度更新**:
- ✅ `prepare-core-npm-publish` archived (yesterday)
- ✅ npm publish (this entry)
- ✅ git tag v0.1.0-core
- ⏳ `migrate-m2nd-to-published-core` — 下個 change，但必須在 **track-m2 worktree** 跑（per project.md session discipline rule）。內容：`apps/medexam2-hospital-tw/package.json` 的 `@study-rpg/core` dep key 從 `workspace:*` 改 `^0.1.0`、`pnpm install` 重 resolve、Chrome MCP smoke 確認 hospital app 仍正常
- ⏳ `add-toefl-mini-demo` — M3 forkability prove-out（50Q TOEFL content pack consume published `@study-rpg/core`），可在 main worktree 跑

**M3 forkability lock 標準**:
1. `pnpm add @study-rpg/core` 從 npm 拉得到 ✓ (本 entry)
2. 二階 fork 改用 published 版本 work ✓ (待 migrate-m2nd)
3. 至少一個外部 demo content pack 接得上 ✓ (待 add-toefl-mini-demo)

完成 1-3 才算 M3 真正 ship。本 entry 只 close item 1。

## 11:40 — M3 unblock prep: core 0.2.0 ready to publish

從 [https://www.npmjs.com/package/@study-rpg/core] 0.1.0 publish 完才發現 migrate-m2nd 卡 SRS extensions blocker（track-m2 commit `59b0a23` 在 core 加 8 個 SRS exports，0.1.0 沒這些）。修法 = bump 0.2.0、merge track-m2 進 main、publish 後 m2 才能 switch 到 published pkg。

**本 session 完成的 prep**:
- `git merge track-m2 → main` (`5e3cdd4` merge commit) — 帶進 SRS core extensions + `hospital-srs` capability spec + wire-hospital-srs-queue archive
- `packages/core/package.json` version 0.1.0 → 0.2.0
- `CHANGELOG.md` 加 [0.2.0] section（列 binary-review SRS API、4 tunable constants、3 types、hospital-srs capability spec delta），順手 backfill [0.1.0] header（pre-publish CHANGELOG 結構不完整、`>` blockquote note 指向 git log）
- `pnpm --filter @study-rpg/core build` ✓（dist/index.js 24.91 KB、index.d.ts 33.01 KB）
- `pnpm pack` ✓ tarball 6 files clean（dist 3 + LICENSE + package.json + README.md，無 src/ 洩漏）
- `grep` 確認 `reviewCardBinary` / `SRS_DAILY_CAP` / `STANDARD_INITIAL_INTERVALS` / `BinaryReviewResult` 都進 dist/index.d.ts

**新 spec 入庫**: `openspec/specs/hospital-srs/spec.md` (139L, 8 reqs)。`openspec validate --all` 30/30 pass（多 1 個 capability，從 29 → 30）。

**Owner 待跑（npm publish + tag，session 結束前**:

```bash
npm login    # 剛 npm logout 過，需重新認證
cd packages/core && npm publish
cd /Users/kangweiling/coding-scratch/study-rpg
git tag v0.2.0-core && git push origin v0.2.0-core
```

跑完後再 verify `npm view @study-rpg/core versions` 看到 `0.1.0` + `0.2.0` 兩個版本就過。

**接著 Step 2** (m2 worktree、separate session):
```bash
cd ~/coding-scratch/study-rpg-m2 && git merge main
/opsx:propose migrate-m2nd-to-published-core
# 改 3 個 package.json: workspace:* → ^0.2.0
# apps/medexam2-hospital-tw + packages/content-medexam2-tw + packages/theme-pixel-hospital
pnpm install && pnpm -r typecheck
# Chrome MCP smoke 二階 SPA 三件套
/opsx:archive migrate-m2nd-to-published-core
```

完成 Step 2 → M3 ✓ 3/3 shipped。

**Lesson — Dual-worktree 用 npm 同步的 timing trap**:
原本以為 publish 0.1.0 → migrate m2 是線性。實際上 track-m2 持續演進（wire-hospital-srs-queue 在 publish 後第 2 小時就 archive），所以 m2 想 switch 到 published pkg 時 core 已落後。**正確 cadence**：(a) m2 收尾停一個明確 milestone (b) 同時 merge 進 main + bump 進 main (c) 再 publish (d) 再讓 m2 switch。本次因為 m2 連續 ship 多個 changes（11 個 archive），任何要 publish 的 timing 都會撞 SRS 這種「core 同步擴展」的 change。


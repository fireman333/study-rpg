# Tasks — Eliminate the cross-device R2 412 storm (server-side plan)

> **RESOLUTION (2026-07-10):** The R2 Class-A **cost** storm this change tracks was eliminated by the sibling change `add-presign-put-rate-limit` (Worker Version `5ec7e951`, shipped 2026-06-27; archived at `openspec/changes/archive/2026-07-10-add-presign-put-rate-limit/`). A server-side per-`(user,bundle)` PUT-presign rate-limit + 10s PUT TTL dropped account-wide 412 `PutObject` from ~277/min → **0**. Its 429 attribution probe also settled Open Question #2: the storm was **one neurons player** (`32c66181`) with a stuck old-bundle iOS-Safari tab — **not 二階**. **Net effect on this plan:** the §0 diagnostic and the §1.6 / §2.1 "storm still ~85%, points to Option 2" readouts below are **SUPERSEDED** — the cost gate is satisfied, so Phase 2 (server-side merge DO) is no longer cost-gated; it survives only as an *optional future convergence nicety* (so a conflicted cross-device writer's data lands without waiting for a client reload). This change stays open purely as that Phase-2 tracker.

## 0. Diagnostic — confirm the split (owner-approved)

- [ ] 0.1 Instrument `cloudflare/sync-worker/src/presign.ts` with ONE `console.log(JSON.stringify({op,bundle,u:userSub.slice(0,8)}))` after `bundleKey(...)`. **Owner-approved prod Worker deploy** (`wrangler deploy`), then `wrangler tail` ~90s during a live storm, capture, **revert the log + redeploy**.
- [ ] 0.2 Aggregate the capture: PUT presigns by `bundle` (neurons vs m2 vs bookmarks) + count distinct `u` per bundle + top-N users by PUT-presign count. Record: which app dominates + whether a few heavy multi-device clients carry most volume.
- [ ] 0.3 Write the result into design.md Open Question #2 and pick the architecture (gates §2/§3).

## 1. Phase 1 — client mitigation (low-risk, both apps)

- [x] 1.1 `engine-r2.ts`: `MAX_PUSH_RETRIES` 3 → 1; `pushBundle` return is now a discriminated union (`{status:'pushed'}` | `{status:'deferred', reason:'concurrent-writer'}`) + new `opts.deferOnConflict`. A surviving 412/409 with `deferOnConflict` returns deferred (no throw); WITHOUT it (account-reset) still throws `r2_blob_concurrent_writer_exhausted` (codex G3 confirmed). `engine.pushNow` re-arms `pending` + `schedulePush()` on deferred (codex G2), and crucially does NOT fire `onPushComplete` on deferred (codex G1 — would falsely upsert leaderboard).
- [x] 1.2 `engine-r2.ts`: `jitter()` helper (±30%) on `BACKOFF_MS` sleeps; on the LAST attempt the 412/409 path breaks WITHOUT the now-redundant sleep (Fork D — the jittered debounce is the backoff).
- [x] 1.3 `engine.ts`: `schedulePush` adds ±30% jitter to the debounce timer + clears `pushTimer` in the fired callback; `pushNow` clears a stale timer at the top. `useSync.ts`: default `DEBOUNCE_MS` 3000 → 12000 (env override kept). `beforeunload`/visibility flush unchanged. Fork C: deferred stays `idle`+`lastError=null` (light 🟢) until `MAX_CONSECUTIVE_DEFERS=5` consecutive defers → `state='error'`+marker (🔴) but still re-arms to recover; a landed push resets the streak.
- [x] 1.4 Unit tests: `r2-defer-on-conflict.test.ts` (412 & 409 → deferred after exactly 1 PUT, not 3× burst; reset path still throws; 200 → pushed) + `sync-engine-defer.test.ts` (deferred does NOT fire onPushComplete / record lastPushAt; re-arms; threshold→error; success resets streak). 676 vitest green; `pnpm --filter @study-rpg/neurons-tw typecheck` clean.
- [ ] 1.5 **Coordinate the same tuning into 二階** (`study-rpg-2nd` repo — separate; via session-bus / handoff). The Worker is shared, so neurons-only tuning leaves 二階's share of the storm. → spawned as an owner task-chip targeting that repo.
- [x] 1.6 **Deploy DONE** (2026-06-25): merged track-neurons→main `148cd8b` → CF Pages run `28159720827` green → prod bundle verified live (`index-CLrzp2Pa.js` as of 2026-06-26 — hash bumped by parallel content rebuilds, still carries `sync_deferred_concurrent_writer`; Supabase env `jakdyjxojokyqxeiuukx` baked). **Measurement DONE (2026-06-26 05:00 UTC, ~20h post-deploy)** via per-UTC-hour `PutObject`×`actionStatus` GraphQL (`~/.cf-analytics-token`, acc `43631a35…`):
  - **Matched morning busy window** (UTC 04–07h = 11:00–14:00 Taiwan): **PRE-fix 06-25 = 82.7%** 412 (65,678 PUT / 54,299 err, peak 21,840/hr) → **POST-fix 06-26 03–04h = 85.2%** (31,303 PUT / 26,670 err, peak 21,621/hr). **No step-down** — fraction, peak volume, and absolute err/hr all essentially unchanged vs the 84–90% baseline.
  - Low-traffic window (06-25 15–20h UTC) = 7.8% — consistent with the single-flight "3–5% low" reading; low concurrency masks the storm, so it is NOT the gating signal.
  - **⚠️ CONFOUND — result is NOT cleanly attributable to neurons.** (a) The R2 metric aggregates the **whole `study-rpg-saves` bucket**; GraphQL exposes no object-key/prefix dimension, so neurons (mitigated) and 二階 `m2` (NOT mitigated) writes are summed together. (b) **二階 prod has NO Phase 1 marker** (`med-study-rpg.com/2nd/` bundle `index-DJtAOEQN.js`, 0 hits) — §1.5 port is undeployed. (c) §0 (the presign-instrumented app-level split) was never run. → The unchanged combined storm is fully consistent with "二階 dominates the storm and is still un-fixed", so it does NOT prove neurons Phase 1 failed. A clean neurons-only number requires §0 OR 二階 port deployed.
  - **→ RESOLVED (2026-07-10):** the confound was settled not by splitting this combined metric but by the sibling `add-presign-put-rate-limit`'s 429 attribution probe — attribution = neurons `32c66181`, a single stuck old-bundle iOS-Safari tab; after its 10s-PUT-TTL redeploy the 412 `PutObject` went 277/min → **0**. Neurons Phase 1 (this change) was fine on its own merits; the residual was one un-reloadable tab that only a **server-side** cap could reach (a client fix lands only after a reload; this tab never reloads).

## 2. Phase 2 — server-side merge DO (decision-gated; only if §0/§1 residual is material)

- [ ] 2.1 **GATE:** owner picks the architecture (Option 1-only vs Option 2 server-merge vs Option 3) from design.md, informed by §0.2 + §1.6. If Option 1 suffices, STOP here and archive.
  - **§1.6 readout (2026-06-26, automated):** combined high-traffic 412 residual is **STILL ~85% / ~21k PUT/hr / ~18k err/hr** — materially unchanged from baseline → on its face this points to **Option 2 (server-side merge DO)**, since client-side debounce+retry tuning cannot stop concurrent writers from racing the same If-Match etag. **BUT do not commit to the DO build on this number alone** — it cannot be split between neurons (mitigated) and 二階 (un-mitigated, shares the bucket). **Recommended sequence before picking:** (1) deploy the 二階 Phase 1 port (§1.5 / chip `task_5f4c70cd`); (2) run §0 (presign instrumentation → app-level + per-user split) to learn which app + whether a few multi-device clients carry the storm; (3) re-measure the next matched morning busy window. If §0 shows neurons' own share is already low after Phase 1 → Option 1 may suffice for neurons (archive); if the storm persists per-app even with both apps mitigated → Option 2. **Do NOT archive this change yet** (residual material + attribution open).
  - **→ SUPERSEDED (2026-07-10):** cost gate satisfied by `add-presign-put-rate-limit` (see the RESOLUTION banner at the top). The ~85% figure was the un-split combined metric distorted by one stuck tab, not a live cost problem — it does NOT gate an Option-2 build. **Owner decision (2026-07-10):** keep this change open as the Phase-2 (server-side merge DO) tracker, but Phase 2 is now an *optional* cross-device convergence improvement, not a cost necessity. Whenever Phase 2 is picked up it still opens as a SEPARATE change per §2.2.
- [ ] 2.2 If Option 2: open a SEPARATE OpenSpec change (its own design/spec) for the per-user Durable Object `POST /sync` write path + server-side merge module (shared with the client merge rules) + back-compat bake with the presign path. Do NOT implement it under this plan change.

## 3. Spec + verify

- [x] 3.1 `cloud-sync` spec delta (2 ADDED mechanism-agnostic requirements: bounded retry amplification + cross-device convergence-without-loss) — `openspec validate eliminate-cross-device-r2-412-storm --strict` passes.
- [x] 3.2 Codex diff review (mirror the single-flight gate). Verdict: **SHIP AS-IS** — G1/G2/G3 confirmed in-code, no blocking issues, sync-light threshold→error still re-arms for recovery. 3 P3 test gaps noted (409 path now covered; real-jitter-timer + account-reset-call-site regression left as optional follow-ups).
- [ ] 3.3 `/verify` after the Phase-1 deploy (Chrome MCP smoke: a 412 defers-dirty instead of bursting; sync still converges). (Post-deploy; gated on the merge=deploy confirm.)
